"""SIO core constants — single source of truth (FR-031).

All platform-name string literals MUST import from this module.
No raw "claude-code" string literal may appear in src/sio/ outside this file.
"""

DEFAULT_PLATFORM: str = "claude-code"
