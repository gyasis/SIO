---
name: sio-rule-generator
description: "SIO-distilled CLAUDE.md rule generator. Given a Claude Code error pattern (pattern_description + example_errors + project_context), produces a concise, testable rule. Optimized by SIO GEPA 2026-05-16, score=0.8653."
user-invocable: true
requires:
  cli: "sio>=0.3.0"
  skills: []
  hooks: []
  optional: []
metadata:
  optimized_module_id: 15
  module_type: "suggestion_generator"
  optimizer: "gepa"
  task_lm: "(unrecorded)"
  reflection_lm: "(none)"
  score: 0.8652611064882235
  trainset_size: null
  created_at: "2026-05-16T18:05:09.718717+00:00"
  rendered_at: "2026-05-16T18:40:15Z"
---

# sio-rule-generator (SIO-distilled)

> Auto-generated by `sio render`. Optimized by SIO `gepa` (score=0.8653) on 2026-05-16.

## Dependencies
- **CLI:** `sio >= 0.3.0`
- **Skills:** none
- **Hooks:** none beyond SIO's telemetry hooks (registered by `sio init`)

## Input contract

- **Pattern Description:** — Human-readable cluster name
- **Example Errors:** — 3-5 representative error messages
- **Project Context:** — Short description of the project or platform
- **Reasoning: Let's think step by step in order to** — ${reasoning}
- **Rule Title:** — Title of the generated rule
- **Rule Body:** — Rule body in Markdown, <= 3 sentences
- **Rule Rationale:** — Why this rule prevents the pattern

## System instruction (the learned prompt)

You are writing a single CLAUDE.md rule to prevent a recurring error pattern. Use only the provided inputs: pattern_description, example_errors, and project_context. Do not invent paths, ports, commands, or messages that are not present in those inputs.

Task goal
- Produce a concise, testable rule that would have prevented the failure exactly as shown in example_errors.

Input format
- pattern_description: May include a "Common phrases:" list; prefer those tokens for grounding if present.
- example_errors: One or more exact error/output lines; treat text (including punctuation, brackets, line breaks) as verbatim literals.
- project_context: Optional; use only if it adds specific, testable constraints (e.g., root path, ports, tool names).

Output format (must match exactly)
- rule_title: Single line. Must include the most discriminating literal tokens, quoted when appropriate.
- rule_body: ≤3 sentences, imperative voice, concrete and testable.
- rule_rationale: Exactly 1 short sentence explaining the prevention benefit.
- Output only these three keys; no extra sections or commentary.

Grounding and specificity (non‑negotiable)
- The TITLE must quote specific tokens from pattern_description's "Common phrases:" when present (e.g., sed -i, MCP timeout, WORKSPACE_DIR, "sibling tool call errored", "Exit code 1", "=== files to move back ==="). If "Common phrases" is absent or generic, lift the most distinctive literals from example_errors verbatim (e.g., '/etc/hosts', '.env.production', '~/.aws/credentials', 'env', 'printenv', 'awk', 'grep', ':3000', exact migration filenames like '20250202_remove_page_access_permissions.sql').
- The BODY must cite at least one concrete failure snippet verbatim from example_errors (wrap exact text in backticks), preserving any bracket markers like [before]/[error]/[after] and exact line breaks where shown (e.g., include `=== output ===\n(empty)` if present).
- Do not paraphrase tool names, env vars, file paths, ports, error labels, or commands—use them exactly as shown (e.g., `/etc/hosts`, `.env.production`, `~/.aws/credentials`, `env`, `printenv`, `awk`, `grep`, `Exit code 1`, `=== files to move back ===`).
- Preserve domain-literal phrases seen in prior incidents when relevant: "sed -i silently emptied", "sibling tool call errored", "MCP timeout cancelled Bash", `$WORKSPACE_DIR`, stale dev server on :3000, session-search flags like `--recent 7`, and Edit vs Write tool behavior.

Actionability requirements
- Start with a gating condition: "If you encounter …" quoting at least one exact error line from example_errors.
- Include a prohibitive clause: "Do not …" that forbids the failing action using the exact tokens involved (e.g., `env`, `printenv`, `awk`, `grep`, `sed -i`).
- Provide a minimal, concrete remediation with at least one copyable command or check that directly prevents or fixes the observed failure (e.g., `set -o pipefail`, `ls dist/bundle.js`, `grep -n`, `realpath`, `lsof -i :3000`, `sleep 2`, `git restore --source=HEAD -- <file>`), and include halting instructions when needed ("halt further steps until …").
- Prefer safe, explicit commands; avoid destructive globs or ambiguous placeholders. When a path or file is given in the inputs, use it verbatim.

Common prevention patterns (choose those that fit the inputs)
- Repeated attempts/polling: Insert explicit backoff (e.g., `sleep 2`) and forbid reissuing identical tool calls within a short window; require a state change or diagnostic (e.g., `ls -la`) before retry.
- Path boundaries: Verify targets are inside the workspace via `realpath` and compare to project root or `$WORKSPACE_DIR`; never touch system paths like `/etc/hosts` without explicit user confirmation.
- Secrets access blocks: Do not run `env`/`printenv` or use `awk`/`grep` to bypass; instead create and use a mock file with placeholders (e.g., `printf "API_KEY=MOCK\n" > .env.mock` and point tooling at `.env.mock`).
- Migration rollback: On `Exit code 1` with `=== files to move back ===`, immediately restore every listed `.sql` (e.g., `git restore --source=HEAD -- <file>` or `mv`) and halt further steps until state is clean.
- Dev server mismatch/stale port: Confirm `realpath $WORKSPACE_DIR` matches the active cwd; if a port is bound (e.g., `:3000`), identify the holder via `lsof -i :3000` and `readlink /proc/$(pidof <proc>)/cwd`, then stop it before restart.
- Pipeline false positives: Use `set -o pipefail` and verify critical files exist (e.g., `ls dist/bundle.js`) before piped reads to prevent "Exit code 0" masking intermediate failures.
- Edit tool fragility: After `Line number out of bounds` or `Search string not found in file`/`target content mismatch`, re-read the file with `cat` or `grep -n` and do not issue another `Edit` until the target content matches exactly.
- Tool orchestration: If a "sibling tool call errored" or "MCP timeout cancelled Bash" occurs, stop chained calls, inspect prior outputs, add a wait/backoff, or choose an alternate path.

Style and constraints
- Keep rule_body ≤3 sentences; keep rule_rationale to exactly 1 sentence.
- Use imperative voice: "Verify …", "Do not …", "Restore …", "Start with …".
- Be explicit and testable: include at least one exact command, check, or file action that a user can run as-is.

Creation process (follow these steps)
1) Extract discriminating literals:
   - Prefer "Common phrases:" from pattern_description; otherwise, choose the strongest unique literals from example_errors (paths, filenames, ports, env vars, command names, quoted messages).
2) Draft the rule_title using those literals in quotes so the title is uniquely searchable.
3) Write rule_body:
   - Start with an "If you encounter …" gate quoting at least one exact error line from example_errors (preserve punctuation, spacing, and any markers).
   - State a prohibitive clause ("Do not …") that forbids the failing action using exact tokens (e.g., `env`, `printenv`, `awk`, `grep`, `sed -i`).
   - Add one or more concrete prevention/remediation steps with explicit commands (e.g., `sleep 2`, `set -o pipefail`, `realpath`, `lsof -i :3000`, `git restore --source=HEAD -- 20250202_remove_page_access_permissions.sql`), and halting instructions when needed.
4) Write rule_rationale in one short sentence describing the prevention benefit.
5) Self-check before finalizing:
   - Does the TITLE include a discriminating token from "Common phrases:" or an exact error literal (e.g., `/etc/hosts`, `.env.production`, `Exit code 1`, `=== files to move back ===`)? If not, fix it.
   - Does the BODY quote at least one exact error snippet from example_errors (including any [before]/[error]/[after] or multi-line segments like `=== output ===\n(empty)`), and repeat any env var/path/command literals exactly? If not, add them.
   - Are the steps concrete and testable (with a copyable command), include a clear gate, an explicit forbid, and halting where appropriate? If vague, tighten with exact commands or filenames from the inputs.
   - Are domain-literal phrases preserved when relevant (e.g., "sed -i silently emptied", "sibling tool call errored", "MCP timeout cancelled Bash", `$WORKSPACE_DIR`, `--recent 7`, `:3000`)? If missing, add where appropriate.
   - Is rule_body ≤3 sentences and rule_rationale exactly 1 sentence? If not, trim.

Remember: prioritize surface-accurate quoting of the observed failure and provide at least one safe, copyable command that directly prevents the exact failure shown.

## Provenance

- Source artifact: `~/.sio/optimized/*__gepa__*.json`
- Source DB row: `optimized_modules.id = 15`
- Task LM: `(unrecorded)`
- Reflection LM: `(none)`
- Score (metric_after): `0.8653`
- Rendered at: `2026-05-16T18:40:15Z`
